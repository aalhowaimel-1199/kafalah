# Set this to the server IP or hostname if users open the system from OTHER machines on the network.
# Leave as localhost if you only open it on the server itself.
SERVER_HOST=localhost

# If accessed by IP, list the origins here too (comma separated), e.g.:
# EXTRA_ORIGINS=http://192.168.1.50:8080,http://192.168.1.50:8081
EXTRA_ORIGINS=

SEED_ADMIN_EMAIL=admin@kafalah.sa
SEED_ADMIN_PASSWORD=Kafalah@2026
BETTER_AUTH_SECRET=kafalah_change_this_to_a_long_random_string_0123456789
